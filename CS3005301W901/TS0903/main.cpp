#include <iostream>
#include <vector>

void runProcess(int tCount, int wCount);

int main() {
    using namespace std;

    int translateCount = 0;
    int wordPairsCount = 0;

    while (cin >> translateCount) {
        cin >> wordPairsCount;
    }
}

void runProcess(int tCount, int wCount) {
    using namespace std;

    vector<string>

      for (unsigned int i = 0; i < tCount; i++) {
    }
}

bool checkIsPair(std::string a, std::string b) {
}